<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Premium Collection | Watch Store</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap');

    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>

<body class="bg-[#09090b] text-zinc-100 min-h-screen">

  <% if(error && error.length> 0) { %>
    <div id="flash-message"
      class="fixed top-8 left-1/2 -translate-x-1/2 bg-red-500/90 backdrop-blur-md text-white px-8 py-3 rounded-full shadow-2xl z-50 border border-red-400">
      <%= error %>
    </div>
    <% } %>

      <header class="relative py-16 border-b border-zinc-800 bg-zinc-900/50">
        <div class="max-w-7xl mx-auto px-6 flex flex-col items-center text-center">
          <span class="text-blue-500 font-bold tracking-[0.2em] uppercase text-xs mb-4">Summer 2026</span>
          <h1 class="text-5xl md:text-7xl font-extrabold tracking-tighter text-white mb-6">
            The New <span class="text-zinc-500">Collection</span>
          </h1>
          <p class="max-w-xl text-zinc-400 text-lg leading-relaxed">
            Experience the fusion of precision engineering and timeless aesthetics.
          </p>
        </div>
      </header>

      <div class="max-w-7xl mx-auto px-6 py-12">
        <div class="flex flex-col lg:flex-row gap-12">

          <aside class="w-full lg:w-1/4 lg:sticky lg:top-8 h-fit space-y-10">
            <div>
              <h3 class="text-xs font-black uppercase tracking-widest text-zinc-500 mb-6">Filter by Category</h3>
              <ul class="space-y-4">
                <li class="flex justify-between items-center group cursor-pointer text-white">
                  <span class="group-hover:translate-x-2 transition-transform duration-300">All Collections</span>
                  <span class="text-zinc-600 text-xs">24</span>
                </li>
                <li
                  class="flex justify-between items-center group cursor-pointer text-zinc-400 hover:text-white transition">
                  <span class="group-hover:translate-x-2 transition-transform duration-300">Automatic</span>
                </li>
                <li
                  class="flex justify-between items-center group cursor-pointer text-zinc-400 hover:text-white transition">
                  <span class="group-hover:translate-x-2 transition-transform duration-300">Chronograph</span>
                </li>
              </ul>
            </div>

            <div class="pt-6 border-t border-zinc-800">
              <h3 class="text-xs font-black uppercase tracking-widest text-zinc-500 mb-6">Price Ceiling</h3>
              <input type="range"
                class="w-full h-1.5 bg-zinc-800 rounded-lg appearance-none cursor-pointer accent-blue-500" />
              <div class="flex justify-between mt-2 text-xs text-zinc-500">
                <span>$0</span>
                <span>$10,000</span>
              </div>
            </div>
          </aside>

          <main class="w-full lg:w-3/4">
            <div class="flex justify-between items-end mb-10">
              <p class="text-zinc-500 text-sm font-medium">
                Showing <span class="text-white">
                  <%= products.length %>
                </span> masterworks
              </p>
              <select
                class="bg-zinc-900 border border-zinc-800 text-zinc-300 text-xs rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 outline-none">
                <option>Sort by: Featured</option>
                <option>Price: Ascending</option>
              </select>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-x-6 gap-y-10">
              <% products.forEach(function(product) { %>
                <div class="group">
                  <div
                    class="relative aspect-[4/5] overflow-hidden rounded-3xl bg-[<%=product.panelcolor %>] border border-zinc-800 shadow-2xl transition-all duration-500 group-hover:border-zinc-600">

                    <% if(product.image) { %>
                      <img src="data:image/jpeg;base64,<%= product.image.toString('base64') %>"
                        alt="<%= product.name %>"
                        class="h-full w-full object-contain p-8 group-hover:scale-110 transition-transform duration-700" />
                      <% } %>

                        <div
                          class="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
                          <button
                            class="bg-white text-black text-sm font-bold px-8 py-3 rounded-full hover:bg-blue-500 hover:text-white transition-colors duration-300 transform translate-y-4 group-hover:translate-y-0">
                            View Details
                          </button>
                        </div>
                  </div>

                  <div class="mt-6 flex justify-between items-start px-2">
                    <div>
                      <h3
                        class="text-lg font-bold text-white tracking-tight leading-none group-hover:text-blue-400 transition-colors">
                        <%= product.name %>
                      </h3>
                      <p class="mt-2 text-sm text-zinc-500 font-medium">
                        Edition 2026
                      </p>
                    </div>
                    <div class="text-right">
                      <p class="text-lg font-bold text-[<%=product.textcolor %>] ">$<%= product.price %>
                      </p>
                      <% if(product.discount> 0) { %>
                        <p class="text-xs text-red-500 line-through">$<%= product.price + product.discount %>
                        </p>
                        <% } %>
                    </div>
                  </div>
                </div>
                <% }) %>
            </div>
          </main>
        </div>
      </div>

      <script>
        // Smoothly hide flash message
        const flash = document.getElementById('flash-message');
        if (flash) {
          setTimeout(() => {
            flash.style.opacity = '0';
            setTimeout(() => flash.remove(), 500);
          }, 2000);
        }
      </script>
</body>

</html>